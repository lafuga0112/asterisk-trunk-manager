[vonage_${name}]
type=endpoint
transport=0.0.0.0-udp
context=from-pstn
disallow=all
allow=ulaw,alaw,gsm,g726,g722,h264,mpeg4
aors=vonage_${name}
send_connected_line=false
language=en
outbound_auth=vonage_${name}-auth
user_eq_phone=yes
t38_udptl=no
t38_udptl_ec=none
fax_detect=no
rtp_keepalive=0
trust_id_inbound=no
t38_udptl_nat=no
direct_media=no
rtp_symmetric=yes
dtmf_mode=auto

[vonage_${name}]
type=aor
qualify_frequency=60
contact=sip:vonage_${name}@${server}

[vonage_${name}-auth]
type=auth
auth_type=userpass
password=${password}
username=${username}

[vonage_${name}-reg]
type=registration
transport=0.0.0.0-udp
outbound_auth=vonage_${name}-auth
retry_interval=60
fatal_retry_interval=30
forbidden_retry_interval=30
max_retries=10000
expiration=3600
auth_rejection_permanent=no
line=yes
endpoint=vonage_${name}
server_uri=sip:${server}
client_uri=sip:vonage_${name}@${server}

[vonage_${name}-identify]
type=identify
endpoint=vonage_${name}
match=${server}